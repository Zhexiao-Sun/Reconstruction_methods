model:
  model_path: models/anycam/pretrained_models/anycam_seq8
  checkpoint_path: null
  device: cuda
  ba_refinement: true
  fit_video_config: models/anycam/anycam/configs/eval_cfgs/fit_video.yaml

data:
  segments_dir: dataset/Benchmark
  videos_dir: dataset/wm_videos/anycam
  output_dir: pipelines/anycam/output
  image_size: 336
  target_fps: 0

trajectory:
  axis_order: [0, 2, 1]
  use_scale_factor: false
  scale_factor: 1.0

